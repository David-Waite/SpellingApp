export default async function createIntalData() {
  const wordsWithDefinitions = [];

  for (let i = 0; i < 10; i++) {
    words.push({
      correct: null,
      word: newWordArray[i],
    });
  }
}
